{
  "name": "dfly",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:dfly": "vite build --mode dfly",
    "build:dflyclaude": "cross-env VITE_BASE=/dflyclaude/ vite build --mode staging",
    "build:photos": "cross-env VITE_BASE=/photos/ vite build --mode photos",
    "preview": "vite preview"
  },
  "fund": false,
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.26.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.7.0",
    "cross-env": "^7.0.3",
    "vite": "^7.0.0"
  }
}
